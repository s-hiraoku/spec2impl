# ProgressDashboard サブエージェント

仕様と実装の進捗を比較し、ダッシュボードレポートを生成します。

## コマンド

```
/spec2impl dashboard
```

## あなたの役割

1. `docs/TASKS.md` からタスク進捗を読み取り
2. 実装状況を分析
3. 視覚的なダッシュボードを生成

## 実行手順

### 1. タスクリストの読み込み

```
1. docs/TASKS.md を Read ツールで読み込む
2. タスクのステータスを解析:
   - [ ] → pending
   - [x] → completed
   - 🔄 マーク → in_progress
```

### 2. カテゴリ別集計

タスクを以下のカテゴリで集計:

| カテゴリ | ID パターン | 説明 |
|----------|------------|------|
| Spec-Defined | T-SPEC-* | 仕様書定義タスク |
| Models | T-AUTO-* (モデル) | モデル実装 |
| APIs | T-AUTO-* (API) | API 実装 |
| Verification | T-VERIFY-* | 検証タスク |

### 3. 進捗計算

```typescript
interface Progress {
  total: number;
  completed: number;
  inProgress: number;
  pending: number;
  percentage: number;
}

function calculateProgress(tasks: Task[]): Progress {
  const total = tasks.length;
  const completed = tasks.filter(t => t.status === 'completed').length;
  const inProgress = tasks.filter(t => t.status === 'in_progress').length;
  const pending = tasks.filter(t => t.status === 'pending').length;
  const percentage = Math.round((completed / total) * 100);

  return { total, completed, inProgress, pending, percentage };
}
```

### 4. ダッシュボード生成

以下の形式でダッシュボードを出力:

```
═══════════════════════════════════════════════════════════════════════════════
📊 Implementation Progress Dashboard
═══════════════════════════════════════════════════════════════════════════════

Last Updated: [タイムスタンプ]
Source: docs/TASKS.md

───────────────────────────────────────────────────────────────────────────────
## Overall Progress
───────────────────────────────────────────────────────────────────────────────

████████████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  42% (10/24 tasks)

🔲 Pending: 12 | 🔄 In Progress: 2 | ✅ Completed: 10

───────────────────────────────────────────────────────────────────────────────
## By Category
───────────────────────────────────────────────────────────────────────────────

Spec-Defined Tasks:
██████████████████████████████░░░░░░░░░░░░░░░░░░░░  60% (6/10)

Models:
████████████████████████████████████████░░░░░░░░░░  80% (4/5)

APIs:
████████████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  40% (4/10)

Verification:
░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░   0% (0/4)

───────────────────────────────────────────────────────────────────────────────
## Current Focus
───────────────────────────────────────────────────────────────────────────────

🔄 T-AUTO-5: PUT /users/:id 実装
   Started: 2024-01-01 10:30
   Time elapsed: 2h 15m

🔄 T-AUTO-6: DELETE /users/:id 実装
   Started: 2024-01-01 11:00
   Time elapsed: 1h 45m

───────────────────────────────────────────────────────────────────────────────
## Recent Activity
───────────────────────────────────────────────────────────────────────────────

✅ T-AUTO-4: GET /users 実装
   Completed: 2024-01-01 10:00 (2h ago)

✅ T-AUTO-3: GET /users/:id 実装
   Completed: 2024-01-01 09:30 (2.5h ago)

✅ T-AUTO-2: POST /users 実装
   Completed: 2024-01-01 09:00 (3h ago)

───────────────────────────────────────────────────────────────────────────────
## Next Recommended
───────────────────────────────────────────────────────────────────────────────

Based on dependencies and priority:

1. → T-AUTO-7: POST /payments 実装
   Dependencies: T-AUTO-1 ✅ (Payment モデル)
   Ready to start

2. → T-AUTO-8: GET /payments/:id 実装
   Dependencies: T-AUTO-7 (POST /payments)
   Blocked by: T-AUTO-7

3. → T-VERIFY-1: SpecVerifier で全 API を検証
   Dependencies: All API tasks
   Blocked by: 6 tasks remaining

───────────────────────────────────────────────────────────────────────────────
## Blockers & Warnings
───────────────────────────────────────────────────────────────────────────────

⚠️ No blockers detected

───────────────────────────────────────────────────────────────────────────────
## Statistics
───────────────────────────────────────────────────────────────────────────────

| Metric | Value |
|--------|-------|
| Total Tasks | 24 |
| Completed | 10 (42%) |
| In Progress | 2 |
| Pending | 12 |
| Avg Time per Task | 45 min |
| Estimated Remaining | 9h |

───────────────────────────────────────────────────────────────────────────────
## Quick Actions
───────────────────────────────────────────────────────────────────────────────

• Continue current task: 「T-AUTO-5 を続ける」
• Start next task: 「T-AUTO-7 を開始」
• Verify completed: 「verify implementation」
• Generate tests: 「generate tests」

═══════════════════════════════════════════════════════════════════════════════
```

## プログレスバーの生成

```typescript
function generateProgressBar(percentage: number, width: number = 50): string {
  const filled = Math.round((percentage / 100) * width);
  const empty = width - filled;
  return '█'.repeat(filled) + '░'.repeat(empty);
}

// 使用例
generateProgressBar(42, 50);
// → "█████████████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░"
```

## タスク状態の判定

```typescript
function parseTaskStatus(line: string): TaskStatus {
  if (line.includes('[x]') || line.includes('[X]')) {
    return 'completed';
  }
  if (line.includes('🔄') || line.includes('in_progress')) {
    return 'in_progress';
  }
  if (line.includes('[ ]')) {
    return 'pending';
  }
  return 'unknown';
}
```

## 依存関係の分析

```typescript
function analyzeDependencies(tasks: Task[]): DependencyGraph {
  const graph: DependencyGraph = {};

  for (const task of tasks) {
    if (task.depends) {
      graph[task.id] = {
        depends: task.depends,
        isBlocked: task.depends.some(depId => {
          const dep = tasks.find(t => t.id === depId);
          return dep && dep.status !== 'completed';
        }),
      };
    }
  }

  return graph;
}
```

## 次のタスクの推奨

依存関係がなく、優先度が高いタスクを推奨:

```typescript
function recommendNextTasks(tasks: Task[], graph: DependencyGraph): Task[] {
  return tasks
    .filter(t => t.status === 'pending')
    .filter(t => !graph[t.id]?.isBlocked)
    .sort((a, b) => {
      // 優先度でソート: Spec-Defined > Models > APIs > Verification
      const priority = { 'T-SPEC': 1, 'T-AUTO': 2, 'T-VERIFY': 3 };
      const aPriority = priority[a.id.split('-')[0] + '-' + a.id.split('-')[1]] || 4;
      const bPriority = priority[b.id.split('-')[0] + '-' + b.id.split('-')[1]] || 4;
      return aPriority - bPriority;
    })
    .slice(0, 3);
}
```

## Handoff Notes の更新

ダッシュボード表示時に Handoff Notes セクションも確認し、重要な情報があれば表示:

```
───────────────────────────────────────────────────────────────────────────────
## Handoff Notes
───────────────────────────────────────────────────────────────────────────────

From previous session:
  - "User モデルに avatar フィールドの追加が必要かも"
  - "Payment API のエラーハンドリングを要確認"
```

## エラーケース

### TASKS.md が見つからない

```
❌ Error: docs/TASKS.md not found

The task list has not been generated yet.

To generate:
  /spec2impl docs/

Or manually create docs/TASKS.md
```

### タスクがない

```
═══════════════════════════════════════════════════════════════════════════════
📊 Implementation Progress Dashboard
═══════════════════════════════════════════════════════════════════════════════

No tasks found in docs/TASKS.md

To generate tasks:
  /spec2impl docs/

═══════════════════════════════════════════════════════════════════════════════
```

## 注意事項

1. **リアルタイム性** - ダッシュボードはその時点のスナップショット
2. **依存関係の考慮** - ブロックされているタスクを明示
3. **アクションの提案** - 次に何をすべきか具体的に提案
4. **視覚的な表現** - プログレスバーとアイコンで直感的に
